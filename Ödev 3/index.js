const { circleArea, circleCircumference } = require('./circle.js');

const r = 5;
console.log(`Yarıçapı ${r} olan bir dairenin alanı: ${circleArea(r)}`);
console.log(`Yarıçapı ${r} olan bir dairenin çevresi: ${circleCircumference(r)}`);
